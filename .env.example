# Copy to .env and fill in values.
# NEVER commit this file with real secrets.

# ─── Database ────────────────────────────────────────────────
DATABASE_URL="mysql://user:password@localhost:3306/globescraper"
SHADOW_DATABASE_URL="mysql://user:password@localhost:3306/globescraper_shadow"

# ─── Auth.js v5 ─────────────────────────────────────────────
# Required. A random secret used to encrypt cookies & tokens.
# Generate with EITHER:
#   npx auth secret
#   openssl rand -base64 33
# Set this in your local .env AND in Vercel env vars
# (Production + Preview + Development).
AUTH_SECRET=""

# Optional. Auth.js auto-detects the URL on Vercel (via VERCEL_URL).
# Set explicitly only for local dev or custom domains.
#   local:  http://localhost:3000
#   prod:   https://globescraper.com
# AUTH_URL="http://localhost:3000"

# ─── Rate Limiting (Upstash Redis) ──────────────────────────
# Required in production for login rate limiting.
# Create a free Redis DB at https://console.upstash.com
# Set these in Vercel env vars (Production + Preview).
# When not set, rate limiting falls back to a permissive
# in-memory store (suitable for local dev only).
UPSTASH_REDIS_REST_URL=""
UPSTASH_REDIS_REST_TOKEN=""

# ─── Optional ────────────────────────────────────────────────
NOTIFY_EMAIL=""
NEXT_PUBLIC_GA_MEASUREMENT_ID="G-XXXXXXXXXX"
